# Decision log

| ID | Date | Decision | Alternatives | Evidence/reasoning | Expected outcome | Actual outcome |
|---|---|---|---|---|---|---|
| RQ001 | 2026-08-27 | Begin with a representation-and-spatial-validation study, not generic site detection. | CNN detector; anomaly discovery; cross-domain transfer. | Existing detection work is substantial; public English LiDAR is promising; geographic split validity is both feasible and scientifically important. | A small, defensible study with meaningful positive or negative findings. | Pending. |
| D001 | 2026-08-27 | Select reviewed NHLE scheduled single bowl barrows and the EA 1 m Composite DTM for E001, subject to acquisition-matched backgrounds and controlled coordinates. | Generic scheduled earthworks; unreviewed title labels; easiest available data. | Phase 2A.5 checked 360 official full entries and retained 261 records after geometry, 128 m coverage, and single-signature provenance gates; 12 provisional groups and four nonadjacent holdouts are viable. | A lawful, geographically useful pool without treating title matches as labels. | FINAL GO for Phase 2B planning; no raster, background, split, or model has been built. |
| P001 | 2026-08-27 | Use CPython 3.12 as the reference runtime and support `>=3.12,<3.15` during Phase 1. | Require only 3.12; make 3.14 the sole reference. | Current scientific/geospatial packages publish 3.14 wheels, while 3.12 offers the most conservative reproduction baseline. | Local 3.14 development remains useful without weakening future reproducibility. | Phase 1 verified on CPython 3.14.7; 3.12 verification remains pending. |
| A001 | 2026-08-27 | Use strict TOML configuration and standard-library validation with no Phase 1 runtime dependencies. | YAML plus PyYAML; configuration framework; unvalidated dictionaries. | Python 3.12 includes `tomllib`; typed dataclasses and explicit validation keep the boundary small and auditable. | Reproducible configuration without dependency or framework overhead. | Implemented with path containment and automated tests. |
| M001 | 2026-08-27 | Represent dataset provenance through typed manifests before acquiring data. | Store metadata in prose only; commit sample data. | Machine-readable status, sensitivity, license, CRS, resolution, paths, dates, and checksums make provenance testable. | Prevent templates or planned sources from being misrepresented as acquired data. | Implemented with a clearly fictional `.invalid` example. |
| P002 | 2026-08-28 | Limit Phase 2B runtime additions to NumPy `>=2.5,<3`, Rasterio `>=1.5,<2`, and PyProj `>=3.7,<4`. | Add the full scientific/GeoPandas stack; build GDAL manually; stay standard-library-only. | Official PyPI metadata publishes CPython 3.12 and 3.14 Windows x86-64 wheels for NumPy 2.5.2, Rasterio 1.5.1, and PyProj 3.7.2. Rasterio provides the required GDAL-backed GeoTIFF operations. | A small, wheel-installable raster stack without modelling dependencies. | CPython 3.14 installation, imports, EPSG:27700 resolution, and `pip check` passed; CPython 3.12 reproduction remains outstanding because 3.12 is not installed. |
| D002 | 2026-08-28 | Use a configurable 128 m square at 1 m resolution as the primary E001 terrain patch. | 64, 96, 192, or 256 m patches. | A coordinate-free parse found usable mound diameters in 184/261 accepted official descriptions: median 20 m, 90th percentile 35 m, maximum 55 m. A 64 m square nearly clips the largest mound; 128 m retains local context and room for edge-safe relief filters without admitting as much broad landscape as 192–256 m. No model/test performance informed this choice. | A 128×128 input supports morphology-focused, deterministic preprocessing while remaining configurable. | Raster and visual pilot QA pending. |
| D003 | 2026-08-28 | Acquire positive terrain as bounded 128 m WCS windows, not complete 5 km tiles. | Download every intersected EA 5 km tile; national download. | Five WCS pilots passed; the full plan touches 200 cells (~20 GB uncompressed) but bounded windows project ~17.2 MB raw. Eight sites cross cell boundaries; WCS and synthetic mosaics handle this without manual tile lists. | Reproducible, small positive-terrain acquisition with exact private provenance. | Phase 2B.5 reused five verified pilots and acquired 256 new windows. All 261 passed raw and representation QA; zero request failures or retries. |
| T001 | 2026-08-28 | Freeze four Phase 2B representations: per-patch median-normalized elevation, slope degrees, 315°/45° hillshade, and 16 m-radius local relief. | Multi-directional hillshade and broad feature catalogue. | Each transform is interpretable, uses no learned global statistic, and has a synthetic analytic test. Four views are sufficient for QA without representation fishing. | A small pre-model representation set with no split leakage. | All 261 archives passed deterministic representation QA; 25 private five-view panels were inspected. No model use. |
| D004 | 2026-08-29 | Retain all seven pairs of distinct overlapping positive patches, assign each pair an explicit overlap-component ID, and require whole-component future partition assignment. | Delete one record per pair; ignore overlap; defer grouping until after splitting. | All seven are separate scheduled entries with distinct descriptions/locations, no exact patch checksum duplication, and membership in one provisional geographic group. Two descriptions explicitly identify a pair or line of barrows. | Preserve legitimate nearby positives without allowing shared terrain to leak across a future split. | Seven `retain_grouped` decisions recorded; no record deleted and no split created. |
| D005 | 2026-08-29 | Freeze one provenance- and coarse-geography-matched unlabelled background per positive, with 500 m positive, 250 m Scheduled Monument, and 256 m background-spacing rules. | 2:1 backgrounds; 5:1 stress set; smaller or larger buffers; call unknown terrain negative. | A balanced primary dataset is interpretable and prevents background prevalence dominating the first baseline. The buffers exceed the patch footprint and were fixed before modelling. Official NHLE Scheduled Monument queries provide a lawful but incomplete known-archaeology exclusion. | A deterministic 522-observation dataset without class/provenance or class/coarse-geography count imbalance. | 573 candidates produced 261 QA-passed backgrounds; 312 reason-coded rejections; no cross-class checksum or buffer collision. |
| D006 | 2026-08-29 | Freeze matched 216/14/31 per-class train/development/final-test counts in both a group-aware random condition and a complete-block geographic condition. | Naive patch random; train/test only; one small geographic block; select groups after model results. | Development enables choices without using final results. Final blocks E3/N2 and E5/N4 are nonadjacent, contain 31 positives plus matched backgrounds, and were chosen from data-design properties before modelling. | A fair random-versus-geographic comparison with intact overlap components and an untouched final test. | Both manifests and assignment hashes frozen; zero cross-partition window overlaps; no model run. |
| D007 | 2026-08-29 | Freeze the all-four 4×4-pooled Random Forest as the E001 primary baseline after geographic-development-only selection. | Dummy or L2 Logistic Regression; single normalized-elevation, slope, hillshade, or local-relief input; inspect final test before freezing. | Its development balanced accuracy was 0.821429, 0.035715 above the next candidates and outside the pre-registered 0.02 tie band. Metadata count imbalances were zero and five label permutations averaged 0.528571. | One hash-bound configuration can proceed to a separately approved final evaluation without post-test choice. | Configuration frozen with threshold 0.5; geographic and random final metrics remain uncomputed. |
| D008 | 2026-08-29 | Accept the one-way E001 final baseline result and advance only to a separately pre-registered Phase 2E robustness/stronger-model gate. | Treat the final test as development; tune after errors; stop because the expected geographic drop did not occur; claim discovery. | The frozen geographic balanced accuracy was 0.870968 [0.774194, 0.951613], versus random 0.822581 [0.718750, 0.916667]. Leakage and direct-shortcut audits passed, but only two geographic blocks, curated labels, unlabelled backgrounds, missing independent review, and no CPython 3.12 rerun limit inference. | Preserve E001 as an immutable baseline while testing robustness through new protocols. | GO FOR PHASE 2E ROBUSTNESS / STRONGER MODELS; Phase 2E not started. |
| D009 | 2026-08-29 | Classify the frozen E001 baseline as ROBUST under the pre-committed Phase 2E-A rule and permit only a separately pre-registered Phase 2E-B stronger-model comparison. | Replace the Phase 2D result with cross-validation; select local relief or a new seed; construct prediction-selected hard backgrounds; begin deep learning automatically. | Five score-independent geographic folds averaged 0.823406 (range 0.790000–0.861111); seed-mean range was 0.019364; 50% training averaged 0.836997; direct shortcut audits passed. | Supports a bounded next model-comparison gate while preserving the original confirmatory result and every post-hoc limitation. | GO FOR PHASE 2E-B STRONGER MODELS; Phase 2E-B not started. |
| D010 | 2026-08-29 | Freeze one 59,145-parameter four-channel compact CNN and training protocol before any real E001 CNN execution. | ResNet or pretrained model; architecture search; new folds; early stopping on the outer holdout; immediate training. | A bounded three-block network is sufficient for the first stronger-model comparison. The existing five geographic folds remain hash-bound, while deterministic complete-group internal validation isolates early stopping from every outer holdout. | A reproducible stronger-model setup that cannot silently tune on held-out geography or expose sensitive inputs. | Phase 2E-B0 status `READY_NOT_TRAINED`; Torch/CUDA/GPU smoke test and synthetic tests passed; no real training or CNN metric. |
| D011 | 2026-08-29 | Classify the compact CNN as not justified at the current data scale and retain the Random Forest for any separately approved Phase 2F. | Promote the CNN because it is newer; tune architecture or threshold after results; select the best seed; collect more data before making any recommendation. | All 15 frozen CNN runs completed. Five-fold mean balanced accuracy was 0.700866 versus frozen RF 0.823406, with the CNN weaker on every fold. Seed-mean range was only 0.017016, but all runs early-stopped and aggregate losses showed internal-validation overfitting. | Preserve the simpler, stronger, more interpretable model while treating the CNN result as useful negative evidence. | `CNN NOT JUSTIFIED AT CURRENT DATA SCALE`; `USE RANDOM FOREST FOR PHASE 2F`. No Phase 2F work started. |
| D012 | 2026-08-29 | Freeze a one-domain, ranking-only Random-Forest inference protocol and fit the unchanged model once on all 522 E001 observations after evaluation. | Scan England; threshold scores into sites; expose candidate locations; tune from candidate results; use the CNN. | The Random Forest remained stronger and simpler. A private 5 km domain, 64 m stride, unchanged 4,096 terrain features, deterministic deduplication, blinded three-band review, and aggregate-only reporting provide a bounded first inference gate. | Enables falsifiable failure analysis while separating terrain similarity from archaeological probability and protecting exact locations. | `READY_NO_REAL_SCAN`; coordinate-free synthetic CPU smoke passed; no real domain bound or scored. |
| D013 | 2026-08-30 | Freeze the first controlled domain's complete private score table before ranking interpretation and advance only to blinded morphology review. | Inspect high scores first; cross-check heritage records; alter thresholds; scan a second domain; publish candidate locations. | All 5,929 frozen-grid windows passed QA. The private score-table SHA-256 was fixed before deterministic deduplication yielded 1,159 representatives and the frozen 12/25/25 review queues. | Preserve a score-independent, location-safe handoff in which visual review cannot see model score, rank, band, coordinates, or known-record status. | `READY FOR BLINDED HUMAN MORPHOLOGY REVIEW`; no review, heritage cross-check, tuning, second domain, or discovery claim. |
| D014 | 2026-08-30 | Freeze a 60-pair independent external geographic-validation design in a new 25 km Salisbury Plain cell, with a 50-pair minimum and no model access until a separate dataset-freeze commit. | Use unreviewed Phase 2F candidates; reuse E001 geography without a buffer; choose sample size after scores; change monument class or model. | The pre-score metadata audit found zero E001 positives/backgrounds in the cell, 87 unreviewed probable-title records after a 15 km E001/Phase 2F exclusion, 87/87 nominal 1 m coverage, and 86/87 resolved provenance signatures. | A genuinely new, bounded test of the unchanged RF whose negative result remains reportable. | `READY FOR PHASE 3B EXTERNAL DATASET CONSTRUCTION`; verified positive count is zero and no external RF score exists. |
